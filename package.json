{
  "name": "mock-gambling-site",
  "version": "0.1.0",
  "private": true,
  "description": "Lucky Spin Casino — a standalone mock gambling site that spends Mock Bank money via card swipe or the Connect SDK.",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently -n api,web -c yellow,green \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace server run build && npm --workspace web run build",
    "typecheck": "npm --workspace server run typecheck && npm --workspace web run typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
